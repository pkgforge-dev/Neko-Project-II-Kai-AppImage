#!/bin/false
dir="${XDG_CONFIG_HOME:-$HOME/.config}"
for d in xnp2kai xnp21kai; do
    target_dir="$dir/$d"
    mkdir -p "$target_dir"
    ln -sf "$APPDIR/share/fonts/OTF/TakaoGothic.ttf" "$target_dir/default.ttf"
done
